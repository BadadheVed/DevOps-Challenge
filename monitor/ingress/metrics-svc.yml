apiVersion: v1
kind: Service
metadata:
  name: app-metrics
  namespace: default
spec:
  type: ClusterIP  # Internal only, not exposed to load balancer
  selector:
    app: prometheus-app # Match your app's labels
  ports:
  - name: metrics
    port: 8080  # Or whatever port your app exposes metrics on
    targetPort: 8000