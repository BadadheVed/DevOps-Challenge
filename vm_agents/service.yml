apiVersion: v1
kind: Service
metadata:
  name: vm-agents-service
  namespace: default
  labels:
    app: vm-agents
spec:
  type: ClusterIP
  selector:
    app: vm-agents
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
