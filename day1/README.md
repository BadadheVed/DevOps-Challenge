1.Disk Usage

```bash
df -h
```

-h means the human readable

2. Running Service

```bash
systemctl --type service --status=running
```

3. Memory Usage

```bash
free -h
```

4. CPU Usage

```bash
top -b -n 1
```

-b means the batch thing means it executers in the non interactive mode
